const fieldDataTyp = (window as any).__INITIAL_DATA__ as FieldDataType;
const fieldFormat = (window as any).__INITIAL_DATA__ as FieldFormat;
const controlTyp = (window as any).__INITIAL_DATA__ as ControlType;
const validators = (window as any).__INITIAL_DATA__ as ValidationType
const displayTyp = (window as any).__INITIAL_DATA__ as DisplayType;
const stylePnl = (window as any).__INITIAL_DATA__ as StylePanel;
const toolbarBtn = (window as any).__INITIAL_DATA__ as ToolbarButtons;
const fieldParent = (window as any).__INITIAL_DATA__ as FieldParent;